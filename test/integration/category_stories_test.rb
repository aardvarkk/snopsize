require 'test_helper'

# Tests user stories for categorization functionality
class CategoryStoriesTest < ActionDispatch::IntegrationTest
  # Story: A user has created a few of their own snops, now they want to 
  # categorize them in a way that makes sense
  test "categorizing snop" do
  end

  # Story: A user has some snops in categories. They don't like the categories
  # and want to switch them around. They will delete some categories and reassign
  # snops to some other categories.
  test "moving snops between categories" do
  end
end
