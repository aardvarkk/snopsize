<%# Update the button showing on screen %>
$("<%= '#fave_' + @snop.id.to_s %>").html("<%=j render partial: 'shared/unfavourite', locals: {snop: @snop} %>");

<%# Show the categorization drop-down %>
$("<%= '#categorization_' + @snop.id.to_s %>").show();