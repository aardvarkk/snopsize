<% if @browse_view %>
  $("#snops_browse_view").html("<%=j render partial: 'shared/snop_browse', locals: {current_snop: @snop, snop_list: @snops, close_path: search_path} %>");
  changeToBrowseView();
  reloadClickHandlers();

  // Reload the social media buttons
  reloadSocialMediaButtons();  
<% else %>
  changeToListView();
<% end %>
