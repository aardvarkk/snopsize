<% if params[:browse_view] == "true" %>
  changeToBrowseView("<%=j @snop.id.to_s unless @snop.nil? %>");
  reloadClickHandlers();
  reloadSocialMediaButtons();
<% else %>
  changeToListView();
<% end %>
