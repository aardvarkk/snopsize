<%= render partial: 'shared/common', locals: { snop_list: @snops, current_snop: @snops.first, data_source: resource_path(domain_id: @resource.domain.id, resource_id: @resource.id), title: 'Snops from ' + @url } %>

<%# list_view_path: resource_path, browse_view_path: , snop: @snops.first, browse_view: true, iSortCol_0: params[:iSortCol_0], sSortDir_0: params[:sSortDir_0], sSearch: params[:sSearch]) %>

