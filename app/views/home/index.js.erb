<% if params[:browse_view] == "true" %>
  $("#snops_browse_view").html("<%=j render partial: 'shared/snop_browse', locals: {current_snop: @snop, snop_list: @snops, close_path: root_path} %>");
  changeToBrowseView();
  reloadClickHandlers();
  reloadSocialMediaButtons();

  <%# Update the side nav with new content %>
  sidenav_content = <%= render partial: 'shared/sidenav_snop_browse', locals: { snop: @snop } %>
  alert(html)
  updateSideNav(html)

<% else %>
  changeToListView();
<% end %>
